name: Build
on:
  pull_request:
  push:
    branches:
      - trunk
jobs:
  build:
    uses: shopwareLabs/actions/.github/workflows/build-zip.yml@main
    with:
      extensionName: SwagPlatformDemoData
